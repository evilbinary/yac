import env
let p = get("PATH") in
if p == 0 then 0
else
  if str_len(p) == 0 then 0
  else
    if get("YAC_NO_SUCH_ENV_ZZZ") == 0 then
      if get_or("YAC_NO_SUCH_ENV_ZZZ", "x") == "x" then 42 else 0
    else 0
