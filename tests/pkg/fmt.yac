import fmt
if format("%s=%d", ["n", 3]) == "n=3" then
  if format("%%", []) == "%" then
    if format("%d", [42]) == "42" then
      if format("a", []) == "a" then 42 else 0
    else 0
  else 0
else 0
