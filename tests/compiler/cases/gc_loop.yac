/* Throwaway cons cells; live cons must survive reuse after collect. */
let live = cons(40, cons(2, [])) in
let rec(i, acc) =
  if i == 0 then acc
  else
    let _ = cons(1, cons(2, [])) in
    let _ = gc_collect() in
    rec(i - 1, acc) in
let _ = rec(200, 0) in
nth(live, 0) + nth(live, 1)
