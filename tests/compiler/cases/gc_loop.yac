/* Throwaway cons cells; live result is an int. GC must not smash the frame. */
let rec(i, acc) =
  if i == 0 then acc
  else
    let _ = cons(1, cons(2, [])) in
    let _ = gc_collect() in
    rec(i - 1, acc) in
rec(200, 42)
