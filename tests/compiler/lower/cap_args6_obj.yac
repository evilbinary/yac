/* capRef 回归：对象 ABI 下 6 参调用（5 个寄存器 + 1 个栈传）。
 * 坑：参数改从第 1 个实参寄存器起后，栈参偏移仍是旧的
 * j-6（应为 j-5），第 6 参读到错误槽位。
 * 期望退出码 31。 */
let mk(n) =
  let f6(a, b, c, d, e, g) = a + b + c + d + e + g + n in
  f6(1, 2, 3, 4, 5, 6)
mk(10)
