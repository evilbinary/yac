/* capRef 回归：三层捕获链（内层捕获的闭包本身也带捕获）。
 * 坑：outer_caps 的负标记转 ["cap",k] 缺失时，链式捕获读到
 * 外层帧的垃圾槽位。
 * 期望退出码 17。 */
let g(n) =
  let a1(x) = x + n in
  let a2(y) = a1(y) * 2 in
  let a3(z) = a2(z) + 1 in
  a3(n)
g(4)
