/* Runtime dlopen/dlsym via ccall. Guest programs should import ffi (pkg/ffi).
 * User .so symbols: csym + ccall(ptr, …). */

package rt.ffi
export cload, csym

let cload(path) = ccall("dlopen", path, 256 + 2)

let csym(name) = ccall("dlsym", 0, name)
